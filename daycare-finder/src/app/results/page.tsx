import React from "react";
// import { useRouter } from "next/router";

export default function Results() {
  //   const router = useRouter();
  //   const { city, from, to, count } = router.query;

  // Optional: You can add logic here to handle cases where the parameters are not available,
  // such as redirecting the user or showing a default view.

  return (
    <div>
      {/* <h1>Results for {city}</h1>
      <p>
        From: {from}
        <br />
        To: {to}
        <br />
        Number of Guests: {count}
      </p> */}
    </div>
  );
}
