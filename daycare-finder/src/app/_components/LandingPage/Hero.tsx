import React from "react";
import SearchBar from "../SearchBar";

const Hero = () => {
  return (
    <div className="flex justify-center items-center bg-green-100 h-96">
      <SearchBar />
    </div>
  );
};

export default Hero;
